#include <iostream>
#include <stdio.h>
#include <time.h>
using namespace std;

void main( )
     {
		            char dateStr [9];
			              char timeStr [9];
				                _strdate( dateStr);
						          printf( "The current date is %s \n", dateStr);
							           _strtime( timeStr );
								            printf( "The current time is %s \n", timeStr);
									          }


